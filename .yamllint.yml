---
extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    # GitHub Actions uses `on:` as a key, which yamllint otherwise flags
    check-keys: false
  comments:
    min-spaces-from-content: 1
